# cartabio / Living Systems

Independent desktop-first concept. The sibling helixa-biotech project is read-only.

## Direction

Revision 17 resolves the overlap reported in the collaboration overview and clarifies the solution chapter's reading order. Desktop consultation actions stay in the right editorial column, away from the sticky proposition. Solution-specific need/cooperation/deliverable text sits with its selector on the left; the SAFE cycle status sits directly below its corresponding 3D view on the right. Narrow layouts read selector, visual/cycle, then detail in natural scroll. Three small first-frame posters are captured from the original procedural Three.js scenes, not the historical tissue photo, and remain visible until each offscreen renderer is activated. They are illustrative scene frames, not microscopy or model output.

Revision 16 prototypes the approved Spatial Lens direction across the hero, collaboration overview and solution/SAFE chapter. The existing fixed-section geometry becomes a larger first-viewport visual; pointer response remains, while scroll progress adds reversible camera approach and a second short observation statement. The light collaboration section uses a sticky editorial heading alongside three sequential research-direction rows. In the solutions chapter, the existing SAFE canvas is larger and its four labeling/imaging/removal/next-round states follow desktop reading position; the imaging sweep remains subtly alive within its selected step. Mobile retains natural scrolling and the existing autonomous illustration. The rest of the business, case, team and contact content stays intact. No external component source, new canvas, scientific result or service promise was added. The first-frame fallback, #home return behavior, reduced-motion and global pause remain explicit acceptance points.

Revision 15 replaces the one-shot IntersectionObserver reveal with scroll-position-linked, reversible text and image motion. Entering, passing, scrolling back, and entering again all map to the same visible progress; the hero canvas has a small scroll parallax on desktop. The motion pause and reduced-motion setting keep content fully legible. The Apple iPhone page (https://www.apple.com/iphone/) informs the restrained product-story pacing, without copied assets or layout. Browser regression checks intermediate progress, repeated passes in both directions, desktop/mobile behavior, pause, reduced motion, navigation, and canvas visibility.

Revision 14 makes responsive layouts reflow rather than compress. The hero and commercial type use bounded breakpoint sizes instead of viewport-scaled fonts; the page gutter stops widening beyond the reading width. Solutions, delivery, and technical evidence switch from paired columns to stacked flow when the available width becomes tight. Delivery and technical Three.js views are independent aspect-ratio stages, not absolute full-section canvases behind copy; the mobile hero stage also occupies flow instead of a fixed top offset. ResizeObserver updates each renderer and camera from its actual stage dimensions. Nine viewport/browser-resize checks cover text fit, no horizontal overflow, canvas aspect ratio, and non-overlapping scene/content bounds.

Revision 13 changes the website from desktop chapter-by-chapter wheel interception to one continuous document. The six addressed chapters, four Three.js scenes, solution and ROI controls, modal, and historical-source boundaries remain; a light full-width collaboration overview now follows the hero so the reader sees audience, cooperation direction, and provisional deliverable types before technical detail. Commercial copy is a discussion preview, not a verified service catalogue or sales promise. Text, imagery and selected panels reveal as they enter the viewport; reduced motion and the page pause expose content immediately. Header, hash links, history, keyboard scrolling and footer all use native document scrolling. Apple.com was a reference for product-first pacing and staged content entry, not a copied layout or asset. Legacy deck gesture tests are historical; the current browser regression covers continuous scrolling, navigation/history, interactions, draft download, responsive layouts and canvas pixels.

Revision 12 adds restrained pointer feedback based on the reviewed [21st Highlight Button](https://21st.dev/@radiumcoders/components/highlight-button) and [Interactive Hover Links](https://21st.dev/@hurerag24/components/interactive-hover-links) patterns. The existing native buttons and links receive local spotlight and underline behavior without React/Tailwind dependencies. In the spatial solution view, an original Three.js nearest-neighbor overlay connects existing illustrative tissue centers and emphasizes the center nearest the cursor, following the interaction idea of [ThreeUI Topology Field](https://threeui.com/three-js/structure-flow/topology-field). These connections are schematic, not inferred cell communication or experimental data.

Revision 11 restores damped pointer-driven camera angles and lighting in all four tissue scenes. The fixed specimen geometry remains unchanged. The solutions chapter now pairs research-question tabs with one readable detail panel and a larger unframed WebGL scene; selection switches between fluorescence, contours and a schematic region. Keyboard selection, global pause, reduced motion and chapter transitions remain supported. Links and controls share hover, focus and pressed feedback. ThreeUI's Topology Field and component catalog informed spatial depth and circular-button feedback (https://threeui.com/three-js/structure-flow/topology-field and https://threeui.com/); no external component source or paid asset is included.

Revision 9 shifts the page from a technique-led experience to a commercial conversation for research services and co-development. The six views now follow the reader's decision path: company proposition, research-question-led solution directions, a collaboration and deliverables draft, SAFE/AI technical foundation, source-qualified research archive, and team/contact. The signature tissue scene remains the visual through-line, but business information uses bounded lists and full-width process rows rather than treating every section as an abstract animated statement. The only primary action is a project-consultation draft; it remains local and does not imply an inbound service endpoint.

The solution directions and deliverable examples are explicitly previews pending project confirmation. They avoid prices, timelines, sample requirements, throughput, acceptance criteria, performance claims and client results. SAFE remains a fixed-specimen staining/imaging/signal-removal method illustration, while AI selection remains a coordinate-linked schematic rather than model output. Desktop retains the established 60% scroll handoff; native and short layouts remain scan-friendly normal scroll.

Revision 8 separates the business chapters while retaining the graphite palette, tissue coordinates and six-section narrative. SAFE enlarges a fixed central subset with synchronized explanatory copy and a full-width cycle indicator. Spatial data uses a top-down view with selectable registration, cell-contour and region states. AI keeps that view beside an object map generated from the same schematic coordinates; the three regions select 1, 9 and 21 model objects, not measured cells or inferred biological results. Research and team use clearer Chinese headings, larger supporting text and preserved historical-source labels. The contact remains a local draft.

Slow wheel steps now accumulate across 550 ms of idle time; rollback uses the same interval. Text fades out between 20% and 60%, incoming text appears after commitment at 60%, while the scientific visuals crossfade continuously from 20% through completion. The chapter rail reflects progress. No new runtime dependencies, paid assets, performance-quality reduction or edits to the sibling project.

Revision 7 replaces the trigger-only desktop transition with wheel-progress scrubbing. The six-module business layout and all four tissue scenes remain unchanged. Scroll progress is reversible until 60%; after commitment, continued gesture momentum cannot skip another chapter. Below-threshold release returns to the current chapter. The outgoing view shifts up/down by up to 24% and fades between 20% and 60%; the incoming visual fades from 20% onward. Incoming copy waits until 60% to avoid double-exposed headings. Navigation/keyboard use the same transition path automatically. Uncommitted previews do not change the URL, chapter index or focus. Dialog opening, visibility changes, pause and responsive-mode changes cleanly cancel previews.

Revision 6 implements the user-approved six-module business narrative in the existing independent project. Company positioning connects experiments, data and AI; the overview now reveals fewer fluorescence signals. SAFE retains the same-section cycle; spatial integration retains the multichannel layer key. AI preserves the same 3D tissue, with a schematic ROI linked to three accessible analysis tabs (cell features, neighborhoods, tissue regions) and qualitative input/output descriptions. These are not computed results. Research transitions to the supplied archive image, with provenance limitations beside the image and in a details dialog. Team/cooperation quiets the motion and uses historically mapped portraits, roles and a local contact draft. The user explicitly approved historical material for preview pending public-release confirmation.

All four scene instances share batched geometry and refresh-driven scheduling. No added animation framework, paid assets or legacy project edits. The fourth scene costs one extra draw call for its ROI; static research/team sections stop every scene loop. Typography, colors, full-width composition and threshold transitions remain consistent. Current scope is six section views and four scientific canvases; earlier revision descriptions below are historical.

Revision 5 is performance-only: preserve the Revision 4 content, geometry detail, particle count, palette, layout and render pixel ratio. Bake the fixed specimen's transforms once, reuse its geometry across chapters, and batch meshes/instances/contours by material or readout channel. Render on each available refresh; stop scheduling inactive, paused, background-tab and context-lost scenes. Pointer easing uses elapsed time, so high-refresh displays do not change response speed. The outgoing scene stays active until its fade completes, then relinquishes rendering to the incoming scene. There is no adaptive resolution or visual-quality downgrade in this revision.

Revision 4: the user confirmed SAFE multiplex staining of FIXED cells and tissue sections. The narrative follows one section through specimen context, repeated staining/imaging/signal removal, spatial registration/integration, and research questions. It does not depict longitudinal live-cell observation or prediction. All five sections retain the graphite environment, cool-white typography and restrained multichannel mint/coral/blue accents.

All three Three.js scenes use the same deterministic thin-section coordinates. Individual cells do not move, breathe or deform. Motion comes from the viewing angle, camera entrance, signal appearance/removal, an illustrative image-acquisition sweep and bounded depth separation of readout contours. Depth layers are measurement channels, not different time points. The process indicator follows the scene clock and freezes with global pause or reduced motion. Animation duration, colors, number of cycles and cell layout are illustrative, not assay specifications. Each section owns its canvas and exits as a complete module.

At suitable desktop sizes, GSAP coordinates complete section exit/entry after a wheel threshold. A gesture latch suppresses continued trackpad momentum. Header and navigation stay still; content and its visual enter together. Touch-oriented, narrow and low-height layouts retain native scrolling. Reduced motion removes transition tweens and pauses autonomous figures.

## References and provenance

- ThreeUI Topology Field: https://threeui.com/three-js/structure-flow/topology-field
- ThreeUI Sylva: https://threeui.com/hero/sylva
- References inform depth and network choreography only. No ThreeUI source, paid components, thumbnails or videos are redistributed.
- Three.js scene is original procedural concept art. It is not microscopy, measured data or a validated biological model.
- SAFE method background: https://doi.org/10.1038/s41587-022-01339-6 . This original paper includes live-cell applications; the company's fixed-specimen focus is user-confirmed and must not be confused with the paper's application scope. No throughput, panel capacity, sensitivity, clinical status, compatibility or licensing claims are inferred.
- The supplied tissue image is not yet authenticated as SAFE data. It is labeled as supplied material throughout the site. The old conceptual cell-network film is no longer displayed, since it did not support the fixed-section story.
- Logo, tissue image and cell animation are copied from the user-provided archive previously extracted in the sibling project. Scientific provenance and publication rights remain to be confirmed before public deployment.
- Manrope is self-hosted under its accompanying OFL license. Chinese uses system fonts.

## Scope

Six section views, four independent scientific visuals, ROI-linked analysis perspectives, source-qualified research archive, historical team portraits, contact-draft download, keyboard-accessible dialogs, global scene motion controls, reduced-motion and WebGL fallback.
No fabricated partners, metrics, clinical claims or team identities. Contact is a local draft, not a backend submission.

## Acceptance

Fresh build and unit tests; browser checks at 1440, 1920, 1024 and 390 pixels; zero horizontal overflow; canvas nonblank and frame difference; navigation, view tabs, motion controls, downloads, reduced motion and WebGL fallback. Separate visual review.
